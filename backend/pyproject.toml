[tool.poetry]
name = "cacapreco-backend"
version = "1.1.0"
description = "Backend for Caça-Preço App"
authors = ["Your Name <you@example.com>"]

[tool.poetry.dependencies]
python = "^3.9"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
